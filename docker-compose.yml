services:
  app:
    build: .
    network_mode: "host"
    volumes:
      - .:/app
    environment:
      - CHROME_BIN=/usr/bin/chromium
      - CHROMEDRIVER_PATH=/usr/bin/chromedriver
      - TZ=Asia/Kolkata
    restart: on-failure:3
